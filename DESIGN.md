# BORO Raid — 설계 문서

> 친구들과 함께 즐기는 **웹 기반 실시간 협동 레이드 게임**
> 최종 수정: 2026-07-24

---

## 1. 한 줄 요약

웹 브라우저에서 돌아가는, **5~10명이 실시간으로 함께 보스를 잡는 탑다운 2D 협동 레이드 게임**.
WoW 레이드 / 펠로우십(협동 파티) 장르의 핵심 재미 — **포지셔닝 · 역할 분담 · 쿨기 타이밍** — 을 웹에서 구현하는 것이 목표.

---

## 2. 핵심 컨셉

- **세계관**: 판타지
- **시점**: 탑다운 2D
- **인원**: 5~10명 동시 접속 (레이드 파티)
- **플레이 흐름**:
  1. 로비(레이드 룸)에 모인다
  2. 역할(직업)을 고른다 → **탱커 / 힐러 / 딜러** (신성 트리니티)
  3. 예고 패턴(telegraph)을 쓰는 보스를 협동으로 공략한다
- **핵심 재미**: 바닥 장판 피하기, 위치 선정, 역할 분담, 쿨다운 타이밍, 광폭화 전 처치

### 왜 3D가 아니라 2D 탑다운인가
3D는 에셋·물리·네트워크 부담이 몇 배로 커진다. 레이드 재미의 대부분인 "장판 피하기 · 위치 선정 · 쿨기 타이밍"은 탑다운 2D로 충분히 살릴 수 있다. 처음엔 2D가 압도적으로 현실적이며, 필요 시 추후 3D 전환도 열려 있다.

---

## 3. 가장 큰 리스크

> **실시간 + 5~10명 동시접속의 네트워크 동기화**가 이 프로젝트에서 가장 어려운 부분이다.

그래픽이 아니라 상태 동기화가 진짜 난관이므로, 이를 잘 다뤄주는 검증된 프레임워크(Colyseus)를 사용해 리스크를 낮춘다.

---

## 4. 기술 스택

| 영역 | 선택 | 이유 |
|------|------|------|
| 언어 | **TypeScript** (server + client + shared) | 타입 공유로 서버/클라 재사용, 버그 감소 |
| 멀티플레이 서버 | **Colyseus** | 실시간 상태 동기화·룸·권한 서버를 프레임워크가 대신 처리 |
| 렌더링 | **PixiJS** (2D) | 웹 2D 게임 표준, 빠르고 안정적 |
| 네트워크 모델 | 권한 서버(authoritative) + 클라 예측/보간 | 치팅 방지 + 부드러운 움직임 |
| 서버 틱레이트 | 20 tick/s (예정) | 레이드 예고 패턴(윈드업 ~1s)에 충분히 반응적 |

### 프로젝트 구조 (예정)
```
/
├── packages/
│   ├── shared/    # 서버·클라 공유 타입, 상수, 게임 로직
│   ├── server/    # Colyseus 룸, 권한 시뮬레이션
│   └── client/    # PixiJS 렌더링, 입력, 예측/보간
├── DESIGN.md
└── README.md
```

---

## 5. 게임 시스템 설계

### 5.1 역할 (신성 트리니티)
| 역할 | 핵심 기능 | 대표 스킬(예정) |
|------|----------|----------------|
| **탱커** | 보스 어그로 유지, 피해 흡수 | 도발, 방어 태세, 광역 어그로 |
| **힐러** | 파티 생존 유지 | 단일 치유, 광역 치유, 보호막 |
| **딜러** | 보스 처치(광폭화 전 DPS) | 단일기, 광역기, 쿨다운 폭딜기 |

### 5.2 전투 자원
- **체력(HP)**: 0이 되면 전투 불능
- **자원(마나/기력 등)**: 스킬 사용 비용
- **쿨다운**: 스킬별 재사용 대기시간

### 5.3 보스 예고 패턴 (telegraph)
- **바닥 장판(AoE)**: 붉은 원/부채꼴이 예고된 뒤 폭발 → 피해서 이동
- **브레스/광역기**: 넓은 범위 공격
- **쫄(adds) 소환**: 추가 몹 처리 필요
- **광폭화 타이머(enrage)**: 제한 시간 내 미처치 시 파티 전멸 → DPS 체크

---

## 6. 단계별 로드맵

| Phase | 목표 | 성공 기준 |
|-------|------|----------|
| **0. 뼈대** | TS 모노레포 + Colyseus 룸 1개 + 접속 확인 | 클라가 서버 룸에 접속됨 |
| **1. 같이 움직이기** ⭐ | 여러 명이 실시간으로 캐릭터를 움직이고 서로 보임 | 2명 이상 접속해 서로의 움직임이 부드럽게 보임 (**최대 관문**) |
| **2. 전투 기본기** | HP/자원, 기본 공격, 쿨다운 스킬, 몹 처치 | 몹 하나를 협동으로 잡음 |
| **3. 역할 3종** | 탱커/힐러/딜러 구현 | 트리니티가 실제로 작동 |
| **4. 보스 & 예고 패턴** 🐉 | 장판/브레스/쫄소환/광폭화 | 파티가 보스를 공략 가능 |
| **5. 로비 & 다듬기** | 룸 생성/참가, 직업 선택 UI, 사운드, 밸런싱 | 처음 접한 친구도 플레이 가능 |
| **6. 배포** | 서버 호스팅 + 링크 공유 | 친구들이 링크로 바로 접속 |

> **Phase 1이 프로젝트의 핵심 관문.** 여기까지 되면 기술적 리스크의 대부분이 해소된다.

---

## 7. 열린 질문 / 추후 결정

- 세계관 디테일 (테마, 보스 컨셉, 아트 방향)
- 캐릭터 커스터마이징 범위
- 진행/보상 시스템 (레벨업, 아이템) 도입 여부
- 여러 보스/스테이지 확장 여부
- 배포 호스팅 선택 (서버 상시 구동 필요)

---

## 8. 개발 방식

- 코딩은 주로 Claude가 담당, 사용자는 방향 제시 및 테스트
- 각 Phase 완료 시 실제 플레이로 검증 후 다음 단계 진행
